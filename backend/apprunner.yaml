version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm install
      - npm run build
  env:
    - name: NODE_ENV
      value: production

run:
  command: node dist/main
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
